syntax = "proto3";

package cryptopals;

enum BytesEncodedFormat {
  BYTES_ENCODED_FORMAT_UNSPECIFIED = 0;
  HEX = 1;
  BASE64 = 2;
  RAW = 3;
}

enum CipherAction {
  CIPHER_ACTION_UNSPECIFIED = 0;
  ENCRYPT = 1;
  DECRYPT = 2;
  CRACK = 3;
}
